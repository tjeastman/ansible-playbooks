---
- name: configure Hadoop cluster nodes
  template: src={{ item }} dest={{ hadoop_config_directory }}
  with_items:
    - core-site.xml
    - hdfs-site.xml
    - mapred-site.xml
    - yarn-site.xml
    - hadoop-env.sh
    - yarn-env.sh
    - hadoop-metrics.properties
- name: copy Hadoop data node file
  template: src={{ hadoop_datanode_file }} dest={{ hadoop_config_directory }}
